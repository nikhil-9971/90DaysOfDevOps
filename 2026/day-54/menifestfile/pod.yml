apiVersion: v1
kind: Pod
metadata:
  name: my-busybox-app
spec:
  containers:
  - name: busybox
    image: busybox:1.28
    command: ["/bin/sh", "-c", "env && sleep 3600"]
    envFrom:
      - configMapRef:
          name: app-config