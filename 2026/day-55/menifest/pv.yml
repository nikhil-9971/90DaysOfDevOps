apiVersion: v1
kind: PersistentVolume
metadata:
  name: persistent-pv
  labels:
    app: my-busybox-app
spec:
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  hostPath:
    path: /tmp/k8s-pv-data